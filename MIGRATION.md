# Migration Guide

Disable the following WPCode snippets once this plugin is active:

- Habaq MVP: CPTs + Taxonomies + Roles/Access Zones + Job fields shortcode + Apply shortcode
- Habaq Job Applications (form handling, CV upload, emails)
- Habaq Job Filters (jobs archive filter UI + query logic)
- Habaq Job Applications Admin Viewer (metaboxes + list table columns)
